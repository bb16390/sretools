# Checklist

- [ ] TradeDayCache 类正确实现 `trade_days: set[date]` 缓存属性
- [ ] `fetch_trade_days()` 通过 CentralClient 请求服务端数据，解析并更新缓存
- [ ] `is_trade_day(date)` 正确判断指定日期是否在交易日集合中
- [ ] `start_refresh_timer()` 启动7200秒定时刷新任务
- [ ] 服务端不可用时降级使用旧缓存
- [ ] 无网络且无缓存时默认所有日期为交易日，并记录警告日志
- [ ] CronParser 支持 `1-5:T` 格式解析（周一至周五的交易日）
- [ ] CronParser 支持 `*:T` 格式解析（仅在交易日）
- [ ] CronParser 支持 `*:F` 格式解析（非交易日）
- [ ] 标准 cron 格式（无 T/F 标记）保持向后兼容
- [ ] DatabaseCollectorTask 配置中正确处理 `trade_day_only` 字段
- [ ] DatabaseCollectorTask 在 `trade_day_only: true` 时调用 TradeDayCache 校验
- [ ] 非交易日时正确跳过执行并记录日志
- [ ] Worker 启动时正确初始化 TradeDayCache
- [ ] TradeDayCache 被正确注入到 TaskScheduler
- [ ] CentralClient 支持 `trade_day_query` 消息类型
- [ ] CentralClient 收到 `trade_day_data` 响应后正确更新 TradeDayCache
- [ ] 为 TradeDayCache.is_trade_day() 编写了单元测试
- [ ] 为 CronParser T/F 标记解析编写了单元测试
- [ ] 为交易日跳过逻辑编写了集成测试
